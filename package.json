{
  "name": "bloodline",
  "version": "0.1.0",
  "private": true,
  "description": "BloodLine — Blood Bank Management System (monorepo)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @bloodline/db generate",
    "db:migrate": "pnpm --filter @bloodline/db migrate",
    "db:migrate:deploy": "pnpm --filter @bloodline/db migrate:deploy",
    "db:push": "pnpm --filter @bloodline/db push",
    "db:seed": "pnpm --filter @bloodline/db seed",
    "db:validate": "pnpm --filter @bloodline/db validate",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.3",
    "typescript": "^5.6.2"
  }
}
